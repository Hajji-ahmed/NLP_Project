# --- 🔐 Secrets (CRITICAL) ---
# Ignores the .env file inside the web folder
web/.env
# Ignores any .env file anywhere in the project (safety net)
.env
.env.*

# --- 🏋️ Heavy Model Files ---
# Ignores the entire models folder inside backend
backend/models/
# Also ignores any stray GGUF files if you move them
*.gguf
*.bin
*.pth

# --- 📝 Logs ---
# Ignores the specific logs folder and log files
backend/logs/
*.log

# --- 🐍 Python & Gradio Junk ---
# Ignore the .gradio folder visible at the top
.gradio/
# Ignore Python cache (seen in api/__pycache__)
__pycache__/
*.pyc

# --- 🔧 Virtual Environments (Standard practice) ---
.venv/
venv/
env/